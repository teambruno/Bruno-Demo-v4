.DS_Store
node_modules/
secrets.json
.env
*.log

# Rendered from ci/azure-keyvault-service-principal.env.tpl - CI writes it to
# RUNNER_TEMP, but a local `cp` of the template lands here. Only the .tpl and
# the (credential-free) ci/azure-keyvault-cli-auth.env belong in git.
ci/azure-keyvault-service-principal.env
reports/*.html
reports/*.json
reports/*.xml

# Collections generated from api-specs/ by api-specs/tools/oas-to-bruno.mjs.
# Build artifacts of the specs - regenerate with `npm run generate:petstore`.
generated/
