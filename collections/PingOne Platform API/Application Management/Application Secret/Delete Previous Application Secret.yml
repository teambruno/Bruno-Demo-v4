info:
  name: Delete Previous Application Secret
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Previous Application Secret](https://developer.pingidentity.com/pingone-api/platform/applications/application-secret/delete-previous-application-secret.html).
