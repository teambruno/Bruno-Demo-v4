info:
  name: Read Application Secret
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Application Secret](https://developer.pingidentity.com/pingone-api/platform/applications/application-secret/read-application-secret.html).
