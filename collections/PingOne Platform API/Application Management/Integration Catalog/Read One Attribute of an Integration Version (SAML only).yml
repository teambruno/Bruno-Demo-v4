info:
  name: Read One Attribute of an Integration Version (SAML only)
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/integrations/{{integrationID}}/versions/{{integrationVersionID}}/attributes/{{attributeID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Attribute of an Integration Version (SAML only)](https://developer.pingidentity.com/pingone-api/platform/integration-catalog/get-one-integration-version-attribute.html).
