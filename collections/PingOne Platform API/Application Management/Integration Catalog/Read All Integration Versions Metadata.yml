info:
  name: Read All Integration Versions Metadata
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/integrations/{{integrationID}}/versions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Integration Versions Metadata](https://developer.pingidentity.com/pingone-api/platform/integration-catalog/get-integration-versions.html).
