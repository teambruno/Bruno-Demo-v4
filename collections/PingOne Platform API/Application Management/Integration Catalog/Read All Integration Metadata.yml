info:
  name: Read All Integration Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/integrations"
  params:
    - name: filter
      value: (name co "SAML")
      type: query
      disabled: true
    - name: expand
      value: versions
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Integration Metadata](https://developer.pingidentity.com/pingone-api/platform/integration-catalog/get-integrations.html).
