info:
  name: Read All Attributes of an Integration Version (SAML only)
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/integrations/{{integrationID}}/versions/{{integrationVersionID}}/attributes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Attributes of an Integration Version (SAML only)](https://developer.pingidentity.com/pingone-api/platform/integration-catalog/get-integration-version-attributes.html).
