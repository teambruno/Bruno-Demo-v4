info:
  name: Read One Application
  type: http
  seq: 13

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Application](https://developer.pingidentity.com/pingone-api/platform/applications/applications-1/read-one-application.html).
