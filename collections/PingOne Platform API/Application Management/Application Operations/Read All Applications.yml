info:
  name: Read All Applications
  type: http
  seq: 12

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Applications](https://developer.pingidentity.com/pingone-api/platform/applications/applications-1/read-all-applications.html).
