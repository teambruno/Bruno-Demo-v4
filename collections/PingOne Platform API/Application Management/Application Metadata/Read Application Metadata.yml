info:
  name: Read Application Metadata
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/metadata"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Application Metadata](https://developer.pingidentity.com/pingone-api/platform/applications/application-metadata/read-application-metadata.html).
