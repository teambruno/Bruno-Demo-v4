info:
  name: Read One Application Role Assignment
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/roleAssignments/{{roleAssignmentID}}"
  params:
    - name: expand
      value: attributes
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Application Role Assignment](https://developer.pingidentity.com/pingone-api/platform/applications/application-role-assignments/read-one-application-role-assignment.html).
