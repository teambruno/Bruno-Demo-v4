info:
  name: Delete Application Attribute Mapping
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/attributes/{{samlAttrID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Application Attribute Mapping](https://developer.pingidentity.com/pingone-api/platform/applications/application-attribute-mapping/delete-application-attribute-mapping.html).
