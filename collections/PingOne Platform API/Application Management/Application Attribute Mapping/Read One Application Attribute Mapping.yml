info:
  name: Read One Application Attribute Mapping
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/attributes/{{samlAttrID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Application Attribute Mapping](https://developer.pingidentity.com/pingone-api/platform/applications/application-attribute-mapping/read-one-application-attribute-mapping.html).
