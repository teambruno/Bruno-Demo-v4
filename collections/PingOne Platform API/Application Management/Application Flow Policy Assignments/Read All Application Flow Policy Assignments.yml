info:
  name: Read All Application Flow Policy Assignments
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/flowPolicyAssignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Application Flow Policy Assignments](https://developer.pingidentity.com/pingone-api/platform/applications/application-flow-policy-assignments/read-all-flow-policy-assignments.html).
