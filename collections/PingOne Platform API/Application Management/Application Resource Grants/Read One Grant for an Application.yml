info:
  name: Read One Grant for an Application
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/grants/{{grantID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Grant for an Application](https://developer.pingidentity.com/pingone-api/platform/applications/application-resource-grants/read-one-grant-for-an-application.html).
