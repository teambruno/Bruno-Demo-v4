info:
  name: Update Grant
  type: http
  seq: 4

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/grants/{{grantID}}"
  body:
    type: json
    data: |-
      {
          "resource": {
              "id": "{{resourceID}}"
          },
          "scopes": [
              {
                  "id": "{{scopeID}}"
              },
              {
                  "id": "{{scopeID_2}}"
              }
          ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Grant](https://developer.pingidentity.com/pingone-api/platform/applications/application-resource-grants/update-grant.html).
