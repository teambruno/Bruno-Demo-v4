info:
  name: Read All Grants for an Application
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/grants"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Grants for an Application](https://developer.pingidentity.com/pingone-api/platform/applications/application-resource-grants/read-all-grants-for-an-application.html).
