info:
  name: Read All MFA Push Credentials
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/pushCredentials"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All MFA Push Credentials](https://developer.pingidentity.com/pingone-api/platform/applications/application-mfa-push-credentials/read-all-mfa-push-credentials.html).
