info:
  name: Update MFA Push Credential
  type: http
  seq: 6

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/applications/{{appID}}/pushCredentials/{{pushCredID}}"
  body:
    type: json
    data: |-
      {
        "type": "APNS",
        "key": "key.value.updated",
        "teamId": "team.id.updated",
        "token": "-----BEGIN PRIVATE KEY-----\nMIGTAgEA****7HUikGZU\n-----END PRIVATE KEY-----"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update MFA Push Credential](https://developer.pingidentity.com/pingone-api/platform/applications/application-mfa-push-credentials/update-mfa-push-credential.html).
