info:
  name: Delete Subscription
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/subscriptions/{{subscriptionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Subscription](https://developer.pingidentity.com/pingone-api/platform/subscriptions-webhooks/delete-subscription.html).
