info:
  name: Read All Subscriptions
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/subscriptions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Subscriptions](https://developer.pingidentity.com/pingone-api/platform/subscriptions-webhooks/read-all-subscriptions.html).
