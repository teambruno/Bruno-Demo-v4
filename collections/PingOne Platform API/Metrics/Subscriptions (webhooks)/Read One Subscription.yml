info:
  name: Read One Subscription
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/subscriptions/{{subscriptionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Subscription](https://developer.pingidentity.com/pingone-api/platform/subscriptions-webhooks/read-one-subscription.html).
