info:
  name: Read Activity Resources
  type: http
  seq: 3

http:
  method: GET
  url: '{{apiPath}}/environments/{{envID}}/activityResources?filter=resourceType eq "POPULATION"&limit=200'
  params:
    - name: filter
      value: resourceType eq "POPULATION"
      type: query
    - name: limit
      value: "200"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Activity Resources](https://developer.pingidentity.com/pingone-api/platform/audit-activities/get-read-activity-resources.html).
