info:
  name: Read One Audit Activity
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/activities/{{activityID}}"
  params:
    - name: filter
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Audit Activity](https://developer.pingidentity.com/pingone-api/platform/audit-activities/get-one-user-activity.html).
