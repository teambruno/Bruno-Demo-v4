info:
  name: Read Active Identity Counts by Date Range
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/metrics/activeIdentityCounts?filter=startDate%20ge%20%222024-05-01T19:00:00Z%22&limit=10&order=-startDate"
  params:
    - name: filter
      value: startDate%20ge%20%222024-05-01T19:00:00Z%22
      type: query
    - name: limit
      value: "10"
      type: query
      description: 1-100
    - name: order
      value: -startDate
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Active Identity Counts by Date Range](https://developer.pingidentity.com/pingone-api/platform/active-identity-counts/read-active-identity-counts-by-date-range.html).
