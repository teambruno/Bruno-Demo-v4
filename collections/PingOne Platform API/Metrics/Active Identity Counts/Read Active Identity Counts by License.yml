info:
  name: Read Active Identity Counts by License
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/organizations/{{orgID}}/licenses/{{licenseID}}/metrics/activeIdentityCounts?aggregatedBy=calendarMonth&limit=1-1000&order=-startDate"
  params:
    - name: aggregatedBy
      value: calendarMonth
      type: query
      description: calendarMonth, licenseYear
    - name: limit
      value: 1-1000
      type: query
      description: "Default: 12"
    - name: order
      value: -startDate
      type: query
    - name: cursor
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Active Identity Counts by License](https://developer.pingidentity.com/pingone-api/platform/active-identity-counts/read-active-identity-counts-by-license.html).
