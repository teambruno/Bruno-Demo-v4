info:
  name: Delete Inbound Traffic Policy
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/inboundTrafficPolicies/{{trafficPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Inbound Traffic Policy](https://developer.pingidentity.com/pingone-api/platform/inbound-traffic/delete-inbound-traffic-policy.html).
