info:
  name: Read One Inbound Traffic Policy
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/inboundTrafficPolicies/{{trafficPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Inbound Traffic Policy](https://developer.pingidentity.com/pingone-api/platform/inbound-traffic/read-one-inbound-traffic-policy.html).
