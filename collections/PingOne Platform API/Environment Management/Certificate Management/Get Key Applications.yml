info:
  name: Get Key Applications
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}/applications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Get Key Applications](https://developer.pingidentity.com/pingone-api/platform/certificate-management/get-key-applications.html).
