info:
  name: Export Public Key (X509 PEM)
  type: http
  seq: 12

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}"
  headers:
    - name: Accept
      value: application/x-x509-ca-cert
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Export Public Key (X509 PEM)](https://developer.pingidentity.com/pingone-api/platform/certificate-management/export-public-key-x509-pem.html).
