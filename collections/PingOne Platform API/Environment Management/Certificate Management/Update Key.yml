info:
  name: Update Key
  type: http
  seq: 14

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}"
  body:
    type: json
    data: |-
      {
        "default": true,
        "usageType": "SIGNING"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Key](https://developer.pingidentity.com/pingone-api/platform/certificate-management/update-key.html).
