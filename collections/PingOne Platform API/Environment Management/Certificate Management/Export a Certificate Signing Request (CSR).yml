info:
  name: Export a Certificate Signing Request (CSR)
  type: http
  seq: 13

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}/csr"
  headers:
    - name: Accept
      value: application/pkcs10
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Export a Certificate Signing Request (CSR)](https://developer.pingidentity.com/pingone-api/platform/certificate-management/export-a-certificate-signing-request-csr.html).
