info:
  name: Delete Certificate
  type: http
  seq: 17

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/certificates/{{certID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Certificate](https://developer.pingidentity.com/pingone-api/platform/certificate-management/delete-certificate.html).
