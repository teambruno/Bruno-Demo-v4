info:
  name: Export Public Key (PKCS7 DER)
  type: http
  seq: 11

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}"
  headers:
    - name: Accept
      value: application/x-pkcs7-certificates
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Export Public Key (PKCS7 DER)](https://developer.pingidentity.com/pingone-api/platform/certificate-management/export-public-key-pkcs7-der.html).
