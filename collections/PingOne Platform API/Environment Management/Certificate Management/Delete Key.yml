info:
  name: Delete Key
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/keys/{{keyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Key](https://developer.pingidentity.com/pingone-api/platform/certificate-management/delete-key.html).
