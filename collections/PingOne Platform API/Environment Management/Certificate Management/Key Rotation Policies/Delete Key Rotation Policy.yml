info:
  name: Delete Key Rotation Policy
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/keyRotationPolicies/{{krpID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Key Rotation Policy](https://developer.pingidentity.com/pingone-api/platform/certificate-management/key-rotation-policies/delete-key-rotation-policy.html).
