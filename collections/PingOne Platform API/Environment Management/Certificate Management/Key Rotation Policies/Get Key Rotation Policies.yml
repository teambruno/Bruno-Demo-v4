info:
  name: Get Key Rotation Policies
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keyRotationPolicies"
  params:
    - name: filter
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Get Key Rotation Policies](https://developer.pingidentity.com/pingone-api/platform/certificate-management/key-rotation-policies/get-key-rotation-policies.html).
