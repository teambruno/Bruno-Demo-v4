info:
  name: Get Certificate
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/certificates/{{certID}}"
  headers:
    - name: Accept
      value: application/x-x509-ca-cert
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Get Certificate](https://developer.pingidentity.com/pingone-api/platform/certificate-management/get-certificate.html).
