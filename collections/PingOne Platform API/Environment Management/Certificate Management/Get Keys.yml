info:
  name: Get Keys
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/keys"
  params:
    - name: filter
      value: ""
      type: query
      description: algorithm (eq), status (eq)
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Get Keys](https://developer.pingidentity.com/pingone-api/platform/certificate-management/get-keys.html).
