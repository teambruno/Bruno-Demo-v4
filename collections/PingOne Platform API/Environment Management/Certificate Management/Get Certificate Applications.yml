info:
  name: Get Certificate Applications
  type: http
  seq: 10

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/certificates/{{certID}}/applications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Get Certificate Applications](https://developer.pingidentity.com/pingone-api/platform/certificate-management/get-certificate-applications.html).
