info:
  name: Read Branding Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/brandingSettings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Branding Settings](https://developer.pingidentity.com/pingone-api/platform/branding/branding-settings/read-branding-settings.html).
