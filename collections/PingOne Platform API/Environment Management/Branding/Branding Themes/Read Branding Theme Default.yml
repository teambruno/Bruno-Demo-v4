info:
  name: Read Branding Theme Default
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/themes/{{themeID}}/default"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Branding Theme Default](https://developer.pingidentity.com/pingone-api/platform/branding/branding-themes/read-branding-theme-default.html).
