info:
  name: Read Branding Themes
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/themes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Branding Themes](https://developer.pingidentity.com/pingone-api/platform/branding/branding-themes/read-branding-themes.html).
