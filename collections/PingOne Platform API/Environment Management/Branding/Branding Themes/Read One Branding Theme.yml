info:
  name: Read One Branding Theme
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/themes/{{themeID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Branding Theme](https://developer.pingidentity.com/pingone-api/platform/branding/branding-themes/read-one-branding-theme.html).
