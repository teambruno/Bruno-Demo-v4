info:
  name: Migrate Branding Theme
  type: http
  seq: 5

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/themes/{{themeID}}/migrate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Migrate Branding Theme](https://developer.pingidentity.com/pingone-api/platform/branding/branding-themes/migrate-branding-theme.html).
