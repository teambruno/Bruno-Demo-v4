info:
  name: Read All Sign-On Policy Actions
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/signOnPolicies/{{policyID}}/actions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Sign-On Policy Actions](https://developer.pingidentity.com/pingone-api/platform/sign-on-policies/sign-on-policy-actions/read-all-sign-on-policy-actions.html).
