info:
  name: Read One Sign-On Policy Action
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/signOnPolicies/{{policyID}}/actions/{{actionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Sign-On Policy Action](https://developer.pingidentity.com/pingone-api/platform/sign-on-policies/sign-on-policy-actions/read-one-sign-on-policy-action.html).
