info:
  name: Update Sign-On Policy
  type: http
  seq: 4

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/signOnPolicies/{{policyID}}"
  body:
    type: json
    data: |-
      {
          "name": "Updated_Policy",
          "description": "a sign-on policy that has been updated",
          "default": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Sign-On Policy](https://developer.pingidentity.com/pingone-api/platform/sign-on-policies/sign-on-policies-1/update-sign-on-policy.html).
