info:
  name: Read One Sign-On Policy
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/signOnPolicies/{{policyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Sign-On Policy](https://developer.pingidentity.com/pingone-api/platform/sign-on-policies/sign-on-policies-1/read-one-sign-on-policy.html).
