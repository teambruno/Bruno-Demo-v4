info:
  name: Read Custom Email Provider
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings/emailDeliverySettings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Custom Email Provider](https://developer.pingidentity.com/pingone-api/platform/notifications/email-delivery-settings/get_custom_email_provider.html).
