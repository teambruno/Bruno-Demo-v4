info:
  name: Read One Trusted Email Domain
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Trusted Email Domain](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/read-one-trusted-email-domain.html).
