info:
  name: Delete Trusted Email Domain
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Trusted Email Domain](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/delete-trusted-email-domain.html).
