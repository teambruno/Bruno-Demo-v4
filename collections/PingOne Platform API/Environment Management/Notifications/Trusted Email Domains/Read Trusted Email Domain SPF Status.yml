info:
  name: Read Trusted Email Domain SPF Status
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}/spf"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Trusted Email Domain SPF Status](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/read-trusted-email-domain-spf-status.html).
