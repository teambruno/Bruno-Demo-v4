info:
  name: Read MAIL FROM Domain
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}/mailFromDomain"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read MAIL FROM Domain](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/read-mail-from-domain.html).
