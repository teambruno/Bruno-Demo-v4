info:
  name: Delete MAIL FROM Domain
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}/mailFromDomain"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete MAIL FROM Domain](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/delete-mail-from-domain.html).
