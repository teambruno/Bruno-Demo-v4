info:
  name: Read All Trusted Email Domains
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/emailDomains"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Trusted Email Domains](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-domains/read-all-trusted-email-domains.html).
