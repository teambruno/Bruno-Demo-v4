info:
  name: Read Notifications Settings
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Notifications Settings](https://developer.pingidentity.com/pingone-api/platform/notifications/notifications-settings/read-notifications-settings.html).
