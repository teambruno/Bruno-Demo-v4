info:
  name: Activate Trusted Email Address
  type: http
  seq: 4

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}/trustedEmails/{{trustedEmailID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.trustedEmail.activate+json
  body:
    type: json
    data: |-
      {  
         "verificationCode":"avu0w4lt"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Activate Trusted Email Address](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-addresses/activate-trusted-email-address.html).
