info:
  name: Read One Trusted Email Address
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/emailDomains/{{emailDomainID}}/trustedEmails/{{trustedEmailID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Trusted Email Address](https://developer.pingidentity.com/pingone-api/platform/notifications/trusted-email-addresses/read-one-trusted-email-address.html).
