info:
  name: Delete Notification Policy
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/notificationsPolicies/{{notificationPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Notification Policy](https://developer.pingidentity.com/pingone-api/platform/notifications/notification-policies/delete_notification_policy.html).
