info:
  name: Read All Notification Policies
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsPolicies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Notification Policies](https://developer.pingidentity.com/pingone-api/platform/notifications/notification-policies/read_all_notification_policies.html).
