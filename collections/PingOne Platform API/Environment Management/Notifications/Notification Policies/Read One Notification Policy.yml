info:
  name: Read One Notification Policy
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsPolicies/{{notificationPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Notification Policy](https://developer.pingidentity.com/pingone-api/platform/notifications/notification-policies/read_single_notification_policy.html).
