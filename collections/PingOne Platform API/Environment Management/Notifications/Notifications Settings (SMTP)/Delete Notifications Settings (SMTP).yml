info:
  name: Delete Notifications Settings (SMTP)
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings/emailDeliverySettings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Notifications Settings (SMTP)](https://developer.pingidentity.com/pingone-api/platform/notifications/notifications-settings-smtp/delete-notifications-settings-smtp.html).
