info:
  name: Read Notifications Settings (SMTP)
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings/emailDeliverySettings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Notifications Settings (SMTP)](https://developer.pingidentity.com/pingone-api/platform/notifications/notifications-settings-smtp/read-notifications-settings-smtp.html).
