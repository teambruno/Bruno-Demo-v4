info:
  name: Read One Template
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/templates/{{templateName}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Template](https://developer.pingidentity.com/pingone-api/platform/notifications/notifications-templates/read-one-template.html).
