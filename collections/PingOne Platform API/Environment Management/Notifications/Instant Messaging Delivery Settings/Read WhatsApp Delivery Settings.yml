info:
  name: Read WhatsApp Delivery Settings
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings/instantMessagingDeliverySettings/whatsApp"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read WhatsApp Delivery Settings](https://developer.pingidentity.com/pingone-api/platform/notifications/instant-messaging-delivery-settings/read_whatsapp_delivery_settings.html).
