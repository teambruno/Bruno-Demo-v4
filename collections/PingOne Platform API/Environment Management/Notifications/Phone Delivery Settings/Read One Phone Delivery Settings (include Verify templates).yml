info:
  name: Read One Phone Delivery Settings (include Verify templates)
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/notificationsSettings/phoneDeliverySettings/{{twilioVerifyPhoneDeliverySettingsID}}?expand=twilioverifytemplates"
  params:
    - name: expand
      value: twilioverifytemplates
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Phone Delivery Settings (include Verify templates)](https://developer.pingidentity.com/pingone-api/platform/notifications/phone-delivery-settings/read_single_phone_delivery_settings_with_twilio_verify_templates.html).
