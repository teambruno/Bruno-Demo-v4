info:
  name: Read All Resource Attributes
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/attributes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Resource Attributes](https://developer.pingidentity.com/pingone-api/platform/resources/resource-attributes/read-all-resource-attributes.html).
