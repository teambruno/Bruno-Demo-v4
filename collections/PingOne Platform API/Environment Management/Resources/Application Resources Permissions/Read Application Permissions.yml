info:
  name: Read Application Permissions
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/applicationPermissions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Application Permissions](https://developer.pingidentity.com/pingone-api/platform/resources/application-resources-permissions/read-application-permissions.html).
