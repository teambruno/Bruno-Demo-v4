info:
  name: Update Scope
  type: http
  seq: 6

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/scopes/{{scopeID}}"
  body:
    type: json
    data: |-
      {
          "name": "Updated-Scope_{{$timestamp}}",
          "description": "An UPDATED scope"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Scope](https://developer.pingidentity.com/pingone-api/platform/resources/resource-scopes/update-scope.html).
