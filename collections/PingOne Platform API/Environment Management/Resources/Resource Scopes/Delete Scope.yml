info:
  name: Delete Scope
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/scopes/{{scopeID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Scope](https://developer.pingidentity.com/pingone-api/platform/resources/resource-scopes/delete-scope.html).
