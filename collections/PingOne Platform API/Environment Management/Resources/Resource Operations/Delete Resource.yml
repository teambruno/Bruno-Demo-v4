info:
  name: Delete Resource
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Resource](https://developer.pingidentity.com/pingone-api/platform/resources/resources-1/delete-resource.html).
