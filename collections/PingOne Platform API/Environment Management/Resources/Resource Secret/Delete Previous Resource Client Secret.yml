info:
  name: Delete Previous Resource Client Secret
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/secret"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Previous Resource Client Secret](https://developer.pingidentity.com/pingone-api/platform/resources/resource-secret/delete-previous-resource-client-secret.html).
