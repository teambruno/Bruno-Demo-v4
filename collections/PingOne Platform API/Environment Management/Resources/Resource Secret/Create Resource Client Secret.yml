info:
  name: Create Resource Client Secret
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/resources/{{resourceID}}/secret"
  body:
    type: json
    data: |-
      {
          "previous": {
              "expiresAt": "2024-01-02T13:54:34.487Z"
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Create Resource Client Secret](https://developer.pingidentity.com/pingone-api/platform/resources/resource-secret/create-resource-client-secret.html).
