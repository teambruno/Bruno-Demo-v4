info:
  name: Read One Custom Admin Role
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/roles/{{customRoleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Custom Admin Role](https://developer.pingidentity.com/pingone-api/platform/roles/custom-roles/read-one-custom-role.html).
