info:
  name: Delete Custom Admin Role
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/roles/{{customRoleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Custom Admin Role](https://developer.pingidentity.com/pingone-api/platform/roles/custom-roles/delete-custom-role.html).
