info:
  name: Read All Custom Admin Roles
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/roles?filter=%28type+eq+%22CUSTOM%22%29"
  params:
    - name: filter
      value: "%28type+eq+%22CUSTOM%22%29"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Custom Admin Roles](https://developer.pingidentity.com/pingone-api/platform/roles/custom-roles/read-all-custom-roles.html).
