info:
  name: Read One Built-in Admin Role
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/roles/{{roleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Built-in Admin Role](https://developer.pingidentity.com/pingone-api/platform/roles/predefined-roles/read-one-role.html).
