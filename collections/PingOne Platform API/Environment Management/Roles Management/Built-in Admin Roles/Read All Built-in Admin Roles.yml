info:
  name: Read All Built-in Admin Roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/roles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Built-in Admin Roles](https://developer.pingidentity.com/pingone-api/platform/roles/predefined-roles/read-all-roles.html).
