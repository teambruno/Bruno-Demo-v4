info:
  name: Delete One Form
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/forms/{{formID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete One Form](https://developer.pingidentity.com/pingone-api/platform/forms/delete-one-form.html).
