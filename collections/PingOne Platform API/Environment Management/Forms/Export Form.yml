info:
  name: Export Form
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/forms/{{formID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.form.export+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Export Form](https://developer.pingidentity.com/pingone-api/platform/forms/export-form.html).
