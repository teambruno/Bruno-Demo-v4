info:
  name: Read All Gateway Credentials
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}/credentials"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Gateway Credentials](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-credentials/read-all-gateway-credentials.html).
