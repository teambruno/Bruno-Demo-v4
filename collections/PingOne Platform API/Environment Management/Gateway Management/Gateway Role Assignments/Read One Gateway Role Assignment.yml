info:
  name: Read One Gateway Role Assignment
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}/roleAssignments/{{gatewayRoleAssignmentsID}}"
  params:
    - name: expand
      value: role
      type: query
      description: Supported for the role and scope embedded resources.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Gateway Role Assignment](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-role-assignments/read-one-gateway-role-assignment.html).
