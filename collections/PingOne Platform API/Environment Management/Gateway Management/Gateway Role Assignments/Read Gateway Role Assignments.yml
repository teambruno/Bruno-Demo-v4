info:
  name: Read Gateway Role Assignments
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}/roleAssignments"
  params:
    - name: expand
      value: role
      type: query
      description: Supported for the role and scope embedded resources.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Gateway Role Assignments](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-role-assignments/read-gateway-role-assignments.html).
