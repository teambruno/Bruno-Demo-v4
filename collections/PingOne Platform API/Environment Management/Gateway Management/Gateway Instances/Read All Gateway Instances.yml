info:
  name: Read All Gateway Instances
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}/instances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Gateway Instances](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-instances/read-all-gateway-instances.html).
