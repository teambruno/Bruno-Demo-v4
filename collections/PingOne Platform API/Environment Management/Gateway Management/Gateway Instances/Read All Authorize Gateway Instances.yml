info:
  name: Read All Authorize Gateway Instances
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{authorizeGatewayID}}/instances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Authorize Gateway Instances](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-instances/read-all-authorize-gateway-instances.html).
