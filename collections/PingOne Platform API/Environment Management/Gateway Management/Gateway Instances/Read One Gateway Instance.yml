info:
  name: Read One Gateway Instance
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}/instances/{{instanceID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Gateway Instance](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateway-instances/read-one-gateway-instance.html).
