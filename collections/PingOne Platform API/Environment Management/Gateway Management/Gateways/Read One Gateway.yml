info:
  name: Read One Gateway
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{gatewayID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Gateway](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateways/read-one-gateway.html).
