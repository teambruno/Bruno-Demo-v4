info:
  name: Read All Authorize Gateways
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Authorize Gateways](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateways/read-all-authorize-gateways.html).
