info:
  name: Read One Authorize Gateway
  type: http
  seq: 10

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/gateways/{{authorizeGatewayID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Authorize Gateway](https://developer.pingidentity.com/pingone-api/platform/gateway-management/gateways/read-one-authorize-gateway.html).
