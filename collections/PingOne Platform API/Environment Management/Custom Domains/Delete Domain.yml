info:
  name: Delete Domain
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/customDomains/{{customDomainID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Domain](https://developer.pingidentity.com/pingone-api/platform/custom-domains/delete-domain.html).
