info:
  name: Update Domain
  type: http
  seq: 6

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/customDomains/{{customDomainID}}"
  body:
    type: json
    data: |-
      {
          "mtlsEnabled": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Domain](https://developer.pingidentity.com/pingone-api/platform/custom-domains/update-domain.html).
