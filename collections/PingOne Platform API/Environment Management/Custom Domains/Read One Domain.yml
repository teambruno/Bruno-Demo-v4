info:
  name: Read One Domain
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/customDomains/{{customDomainID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Domain](https://developer.pingidentity.com/pingone-api/platform/custom-domains/read-one-domain.html).
