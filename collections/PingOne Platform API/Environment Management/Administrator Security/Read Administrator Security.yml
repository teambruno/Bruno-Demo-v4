info:
  name: Read Administrator Security
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/adminConfig"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Administrator Security](https://developer.pingidentity.com/pingone-api/platform/administrator-security/read-admin-config.html).
