info:
  name: Read One Environment
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}"
  params:
    - name: limit
      value: ""
      type: query
      disabled: true
    - name: expand
      value: bill of materials
      type: query
      disabled: true
    - name: order
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Environment](https://developer.pingidentity.com/pingone-api/platform/environments/read-one-environment.html).
