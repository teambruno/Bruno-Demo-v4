info:
  name: Delete Alert Channel
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/alertChannels/{{alertChannelID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Alert Channel](https://developer.pingidentity.com/pingone-api/platform/alerting/delete-alert-channel.html).
