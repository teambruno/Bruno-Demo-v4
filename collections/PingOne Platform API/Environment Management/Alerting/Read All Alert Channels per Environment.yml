info:
  name: Read All Alert Channels per Environment
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/alertChannels"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Alert Channels per Environment](https://developer.pingidentity.com/pingone-api/platform/alerting/read-all-alert-channels-per-environment.html).
