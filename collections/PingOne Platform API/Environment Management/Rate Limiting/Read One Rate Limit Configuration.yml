info:
  name: Read One Rate Limit Configuration
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/rateLimitIpConfigs/{{configurationID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Rate Limit Configuration](https://developer.pingidentity.com/pingone-api/platform/rate-limiting/read-one-rate-limit-configuration.html).
