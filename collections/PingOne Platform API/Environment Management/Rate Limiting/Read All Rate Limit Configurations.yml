info:
  name: Read All Rate Limit Configurations
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/rateLimitIpConfigs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Rate Limit Configurations](https://developer.pingidentity.com/pingone-api/platform/rate-limiting/read-all-rate-limit-configurations.html).
