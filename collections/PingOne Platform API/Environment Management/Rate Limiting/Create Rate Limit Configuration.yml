info:
  name: Create Rate Limit Configuration
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/rateLimitIpConfigs"
  body:
    type: json
    data: |
      {
        "type" : "WHITELIST",
        "value" : "206.123.34.12"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Create Rate Limit Configuration](https://developer.pingidentity.com/pingone-api/platform/rate-limiting/create-rate-limit-configuration.html).
