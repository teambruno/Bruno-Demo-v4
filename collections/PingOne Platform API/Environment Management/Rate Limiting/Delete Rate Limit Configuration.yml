info:
  name: Delete Rate Limit Configuration
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/rateLimitIpConfigs/{{configurationID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Rate Limit Configuration](https://developer.pingidentity.com/pingone-api/platform/rate-limiting/delete-rate-limit-configuration.html).
