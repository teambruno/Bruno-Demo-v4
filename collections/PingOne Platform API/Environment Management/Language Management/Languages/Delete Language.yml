info:
  name: Delete Language
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/languages/{{languageID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Language](https://developer.pingidentity.com/pingone-api/platform/language-management/languages/delete-language-1.html).
