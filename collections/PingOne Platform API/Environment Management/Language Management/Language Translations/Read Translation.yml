info:
  name: Read Translation
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/translations/{{locale}}"
  params:
    - name: module
      value: flow-ui
      type: query
      description: Specifies the interface module key value. See documentation for usage.
      disabled: true
    - name: block
      value: UsernamePasswordValidator
      type: query
      description: Specifies a key for a logical group of strings, such as a form or a tab.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Translation](https://developer.pingidentity.com/pingone-api/platform/language-management/language-translations/read-translation.html).
