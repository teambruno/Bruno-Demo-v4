info:
  name: Create Translation
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/translations/{{locale}}?module=forms&block=customMessages"
  params:
    - name: module
      value: forms
      type: query
    - name: block
      value: customMessages
      type: query
  body:
    type: json
    data: |-
      {
           "key":"forms.fields.customkey2",
           "translatedText":"customkey1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Create Translation](https://developer.pingidentity.com/pingone-api/platform/language-management/language-translations/create-translation.html).
