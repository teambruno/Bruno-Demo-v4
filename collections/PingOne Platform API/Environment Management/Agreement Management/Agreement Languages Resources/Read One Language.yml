info:
  name: Read One Language
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/agreements/{{agreementID}}/languages/{{languageID}}"
  params:
    - name: expand
      value: currentRevision,revisions
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Language](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreement-languages-resources/read-one-language.html).
