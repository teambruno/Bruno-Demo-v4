info:
  name: Read All Languages
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/agreements/{{agreementID}}/languages"
  params:
    - name: expand
      value: currentRevision,revisions
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Languages](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreement-languages-resources/read-all-languages.html).
