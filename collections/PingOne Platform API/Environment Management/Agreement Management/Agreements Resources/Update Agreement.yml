info:
  name: Update Agreement
  type: http
  seq: 4

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/agreements/{{agreementID}}"
  body:
    type: json
    data: |-
      {
        "name": "Agreement_1_UPDATED_{{$timestamp}}",
        "reconsentPeriodDays": 360
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Agreement](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreements-resources/update-agreement.html).
