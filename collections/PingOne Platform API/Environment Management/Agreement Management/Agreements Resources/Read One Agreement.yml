info:
  name: Read One Agreement
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/agreements/{{agreementID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Agreement](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreements-resources/read-one-agreement.html).
