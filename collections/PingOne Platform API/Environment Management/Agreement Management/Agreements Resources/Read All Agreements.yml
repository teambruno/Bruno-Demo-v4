info:
  name: Read All Agreements
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/agreements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Agreements](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreements-resources/read-all-agreements.html).
