info:
  name: Delete Agreement
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/agreements/{{agreementID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Agreement](https://developer.pingidentity.com/pingone-api/platform/agreement-management/agreements-resources/delete-agreement.html).
