info:
  name: Update Population Default IdP
  type: http
  seq: 6

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/populations/{{popID}}/defaultIdentityProvider"
  body:
    type: json
    data: |-
      {
          "id": "{{providerID}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Population Default IdP](https://developer.pingidentity.com/pingone-api/platform/populations/update-population-default-idp.html).
