info:
  name: Read One Population Default IdP
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/populations/{{popID}}/defaultIdentityProvider"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Population Default IdP](https://developer.pingidentity.com/pingone-api/platform/populations/read-one-population-default-idp.html).
