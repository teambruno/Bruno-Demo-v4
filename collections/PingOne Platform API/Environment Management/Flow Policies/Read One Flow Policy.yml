info:
  name: Read One Flow Policy
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/flowPolicies/{{flowPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Flow Policy](https://developer.pingidentity.com/pingone-api/platform/flow-policies/read-one-flow-policy.html).
