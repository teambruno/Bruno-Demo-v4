info:
  name: Read Recaptcha Configuration
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/recaptchaV2Config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Recaptcha Configuration](https://developer.pingidentity.com/pingone-api/platform/forms-recaptcha/read-recaptcha-configuration.html).
