info:
  name: Read One License Name
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/organizations/{{orgID}}/licenses/{{licenseID}}/name"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One License Name](https://developer.pingidentity.com/pingone-api/platform/licenses/read-one-license-name.html).
