info:
  name: Update One License Name
  type: http
  seq: 4

http:
  method: PUT
  url: "{{apiPath}}/organizations/{{orgID}}/licenses/{{licenseID}}/name"
  body:
    type: json
    data: |-
      {
          "name": "Internal Test License"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update One License Name](https://developer.pingidentity.com/pingone-api/platform/licenses/update-one-license-name.html).
