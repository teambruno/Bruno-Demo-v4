info:
  name: Read All Organizations
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/organizations?cursor="
  params:
    - name: limit
      value: "25"
      type: query
      description: 1-5000
      disabled: true
    - name: cursor
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Organizations](https://developer.pingidentity.com/pingone-api/platform/organizations/read-all-organizations.html).
