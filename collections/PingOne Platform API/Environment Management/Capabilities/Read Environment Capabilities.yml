info:
  name: Read Environment Capabilities
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/capabilities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Environment Capabilities](https://developer.pingidentity.com/pingone-api/platform/capabilities/read-environment-capabilities.html).
