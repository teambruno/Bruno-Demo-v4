info:
  name: Read Organization Capabilities
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/organizations/{{orgID}}/capabilities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Organization Capabilities](https://developer.pingidentity.com/pingone-api/platform/capabilities/read-organization-capabilities.html).
