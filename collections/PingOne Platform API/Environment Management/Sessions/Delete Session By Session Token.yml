info:
  name: Delete Session By Session Token
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/sessions/me"
  headers:
    - name: Cookie
      value: ST={{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Session By Session Token](https://developer.pingidentity.com/pingone-api/platform/sessions/delete-session-me.html).
