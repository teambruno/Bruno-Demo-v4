info:
  name: Update Session By ID
  type: http
  seq: 5

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/sessions/{{sessionID}}"
  body:
    type: json
    data: |-
      {
          "activeAt": "{{$isoTimestamp}}",
          "idleTimeoutInMinutes": "72000",
          "lastSignOn": {
              "authenticators": [
                  "pwd"
              ],
              "remoteIp": "174.1.62.19",
              "withAuthenticator": {
                  "pwd": {
                      "at": "2022-08-17T01:21:30.116Z",
                      "policy": {
                          "id": "{{passwordPolicyID}}",
                          "type": "PINGONE"
                      }
                  }
              }
          },
          "user": {
              "id": "{{userID}}"
          },
          "userAgent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10.15; rv:103.0) Gecko/20100101 Firefox/103.0"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Session By ID](https://developer.pingidentity.com/pingone-api/platform/sessions/update-session-id.html).
