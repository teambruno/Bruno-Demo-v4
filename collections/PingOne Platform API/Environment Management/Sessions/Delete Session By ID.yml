info:
  name: Delete Session By ID
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/sessions/{{sessionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Session By ID](https://developer.pingidentity.com/pingone-api/platform/sessions/delete-session-id.html).
