info:
  name: Read One Identity Provider
  type: http
  seq: 16

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/identityProviders/{{providerID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Identity Provider](https://developer.pingidentity.com/pingone-api/platform/identity-provider-management/identity-providers/read-one-identity-provider.html).
