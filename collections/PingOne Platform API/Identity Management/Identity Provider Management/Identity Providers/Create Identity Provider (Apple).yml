info:
  name: Create Identity Provider (Apple)
  type: http
  seq: 9

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/identityProviders"
  params:
    - name: expand
      value: attributes
      type: query
      description: Shows the details for the core attribute mapping created by the request.
      disabled: true
  body:
    type: json
    data: |-
      {
          "description": "Apple Provider",
          "enabled": true,
          "name": "AppleIdP",
          "type": "APPLE",
          "clientId": "APPLE_IDP",
          "clientSecret": "APPLE_SECRET",
          "clientSecretSigningKey": "-----BEGIN PRIVATE KEY-----\nMIGTAgEAMB....\n-----END PRIVATE KEY-----",
          "teamId": "{{teamID}}",
          "keyId": "{{keyID}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        test("Status code is 201", function() {
            expect(res.getStatus()).to.equal(201);
        });

        test("Response body JSON contains createdAt and id strings", function() {
            const jsonData = res.getBody();
            expect(jsonData.createdAt).to.be.a('string');
            expect(jsonData.id).to.be.a('string');
            bru.setEnvVar("providerID", jsonData.id);
            console.log("providerID is now: " + bru.getEnvVar("providerID"));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Create Identity Provider (Apple)](https://developer.pingidentity.com/pingone-api/platform/identity-provider-management/identity-providers/create-identity-provider-apple.html).
