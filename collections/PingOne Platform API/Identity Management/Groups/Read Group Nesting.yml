info:
  name: Read Group Nesting
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}/memberOfGroups"
  params:
    - name: expand
      value: group
      type: query
      disabled: true
    - name: limit
      value: "100"
      type: query
      description: Default
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Group Nesting](https://developer.pingidentity.com/pingone-api/platform/groups/read-group-member-of.html).
