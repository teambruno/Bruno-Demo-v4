info:
  name: Read One Group
  type: http
  seq: 5

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}?include=totalMemberCounts"
  params:
    - name: include
      value: totalMemberCounts
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Group](https://developer.pingidentity.com/pingone-api/platform/groups/read-one-group.html).
