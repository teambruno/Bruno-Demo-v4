info:
  name: Delete Group
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Group](https://developer.pingidentity.com/pingone-api/platform/groups/delete-group.html).
