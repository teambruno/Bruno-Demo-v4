info:
  name: Delete Group Nesting
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}/memberOfGroups/{{nestedGroupID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Group Nesting](https://developer.pingidentity.com/pingone-api/platform/groups/delete-nested-group.html).
