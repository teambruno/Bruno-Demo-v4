info:
  name: Read All Group Role Assignments
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}/roleAssignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Group Role Assignments](https://developer.pingidentity.com/pingone-api/platform/group-role-assignments/read-all-group-role-assignments.html).
