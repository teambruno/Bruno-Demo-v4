info:
  name: Delete Direct-mapped User
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{scimPath}}/environments/{{envID}}/v2/DirectMappedUsers/{{userID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Direct-mapped User](https://developer.pingidentity.com/pingone-api/platform/scim/direct-mapped-users/delete-direct-mapped-user.html).
