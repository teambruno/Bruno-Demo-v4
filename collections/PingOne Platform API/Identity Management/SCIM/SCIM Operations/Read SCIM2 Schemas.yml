info:
  name: Read SCIM2 Schemas
  type: http
  seq: 2

http:
  method: GET
  url: "{{scimPath}}/environments/{{envID}}/v2/Schemas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read SCIM2 Schemas](https://developer.pingidentity.com/pingone-api/platform/scim/scim-operations/read-scim2-schemas.html).
