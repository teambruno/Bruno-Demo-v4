info:
  name: Read SCIM2 Resource Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{scimPath}}/environments/{{envID}}/v2/ResourceTypes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read SCIM2 Resource Types](https://developer.pingidentity.com/pingone-api/platform/scim/scim-operations/read-scim2-resource-types.html).
