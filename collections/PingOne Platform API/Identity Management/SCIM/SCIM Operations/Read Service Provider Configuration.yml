info:
  name: Read Service Provider Configuration
  type: http
  seq: 3

http:
  method: GET
  url: "{{scimPath}}/environments/{{envID}}/v2/ServiceProviderConfig"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Service Provider Configuration](https://developer.pingidentity.com/pingone-api/platform/scim/scim-operations/read-service-provider-configuration.html).
