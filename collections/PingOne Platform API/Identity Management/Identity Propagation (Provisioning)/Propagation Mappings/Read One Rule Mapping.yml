info:
  name: Read One Rule Mapping
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/rules/{{ruleID}}/mappings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Rule Mapping](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-mappings/read-one-rule-mapping.html).
