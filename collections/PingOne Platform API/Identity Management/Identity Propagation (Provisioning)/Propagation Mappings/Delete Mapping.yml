info:
  name: Delete Mapping
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/propagation/mappings/{{mappingID}}"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Mapping](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-mappings/delete-mapping.html).
