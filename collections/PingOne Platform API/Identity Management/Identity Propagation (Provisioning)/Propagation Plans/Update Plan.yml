info:
  name: Update Plan
  type: http
  seq: 4

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/propagation/plans/{{planID}}"
  body:
    type: json
    data: |-
      {
          "name": "Test-Propagation-Plan"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Plan](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-plans/update-plan.html).
