info:
  name: Read One Plan
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/plans/{{planID}}"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Plan](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-plans/read-one-plan.html).
