info:
  name: Delete Rule
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/propagation/rules/{{ruleID}}"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: deprovision
      value: "true"
      type: query
      description: When equal to true, enables deprovisioning of users when rules and associated stores are deleted.
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Rule](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/delete-rule.html).
