info:
  name: Read One Synced Group for a Rule
  type: http
  seq: 11

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/rules/{{ruleID}}/syncedGroups/{{groupID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Synced Group for a Rule](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-one-synced-group-for-a-rule.html).
