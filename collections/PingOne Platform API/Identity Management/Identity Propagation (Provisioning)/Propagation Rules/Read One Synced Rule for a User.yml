info:
  name: Read One Synced Rule for a User
  type: http
  seq: 7

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/syncedRules/{{ruleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Synced Rule for a User](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-one-synced-rule.html).
