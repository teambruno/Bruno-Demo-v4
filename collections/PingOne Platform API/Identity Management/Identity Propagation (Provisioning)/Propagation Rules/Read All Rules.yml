info:
  name: Read All Rules
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/rules"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Rules](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-all-rules.html).
