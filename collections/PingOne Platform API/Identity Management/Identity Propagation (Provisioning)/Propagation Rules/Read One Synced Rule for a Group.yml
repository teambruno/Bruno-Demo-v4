info:
  name: Read One Synced Rule for a Group
  type: http
  seq: 9

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/groups/{{groupID}}/syncedRules/{{ruleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Synced Rule for a Group](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-one-synced-rule-for-a-group.html).
