info:
  name: Read All Synced Groups for a Rule
  type: http
  seq: 10

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/rules/{{ruleID}}/syncedGroups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Synced Groups for a Rule](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-all-synced-groups-for-a-rule.html).
