info:
  name: Read All Synced Rules for a User
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/syncedRules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Synced Rules for a User](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-rules/read-all-synced-rules.html).
