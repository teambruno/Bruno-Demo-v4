info:
  name: Identity Propagation Store Metadata (Workday)
  type: http
  seq: 13

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/propagation/storeMetadata/Workday"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Identity Propagation Store Metadata (Workday)](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-store-metadata/identity-propagation-store-metadata-workday.html).
