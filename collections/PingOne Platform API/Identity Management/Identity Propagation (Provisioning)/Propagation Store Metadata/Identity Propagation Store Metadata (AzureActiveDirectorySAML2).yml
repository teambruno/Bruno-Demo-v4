info:
  name: Identity Propagation Store Metadata (AzureActiveDirectorySAML2)
  type: http
  seq: 2

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/propagation/storeMetadata/AzureActiveDirectorySAML2"
  body:
    type: json
    data: |-
      {
          "ClientId": "azureADClientId",
          "TenantDomain": "TenantDomain",
          "ClientSecret": "azureADClientSecret",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Identity Propagation Store Metadata (AzureActiveDirectorySAML2)](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-store-metadata/identity-propagation-store-metadata-azure-ad-saml.html).
