info:
  name: Identity Propagation Store Metadata (SalesforceContacts)
  type: http
  seq: 9

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/propagation/storeMetadata/SalesforceContacts"
  body:
    type: json
    data: |-
      {
        "SALESFORCE_DOMAIN": "my.salesforce.com",
        "CLIENT_ID": "yourClientID",
        "CLIENT_SECRET": "yourClientSecret",
        "OAUTH_ACCESS_TOKEN": "yourAccessToken",
        "OAUTH_REFRESH_TOKEN": "yourRefreshToken"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Identity Propagation Store Metadata (SalesforceContacts)](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-store-metadata/identity-propagation-store-metadata-salesforcecontacts.html).
