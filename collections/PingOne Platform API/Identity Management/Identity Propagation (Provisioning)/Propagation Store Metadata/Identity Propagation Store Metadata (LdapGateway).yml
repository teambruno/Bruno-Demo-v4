info:
  name: Identity Propagation Store Metadata (LdapGateway)
  type: http
  seq: 6

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/propagation/storeMetadata/LdapGateway"
  body:
    type: json
    data: |-
      {
          "LDAP_TYPE": "Microsoft Active Directory"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Identity Propagation Store Metadata (LdapGateway)](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-store-metadata/identity-propagation-store-metadata-ldapgateway.html).
