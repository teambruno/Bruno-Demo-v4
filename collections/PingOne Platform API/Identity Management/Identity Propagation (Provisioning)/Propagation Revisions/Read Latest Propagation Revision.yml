info:
  name: Read Latest Propagation Revision
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/revisions/id%3Alatest"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Latest Propagation Revision](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-revisions/read-latest-revision.html).
