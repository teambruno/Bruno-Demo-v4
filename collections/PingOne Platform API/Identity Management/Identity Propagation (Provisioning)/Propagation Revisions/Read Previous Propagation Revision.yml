info:
  name: Read Previous Propagation Revision
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/propagation/revisions/{{previousRevisionID}}"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Previous Propagation Revision](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-revisions/read-previous-revision.html).
