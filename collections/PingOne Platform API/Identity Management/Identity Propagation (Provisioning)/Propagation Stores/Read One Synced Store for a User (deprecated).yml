info:
  name: Read One Synced Store for a User (deprecated)
  type: http
  seq: 19

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/syncedStores/{{storeID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Synced Store for a User (deprecated)](https://developer.pingidentity.com/pingone-api/platform/identity-propagation-provisioning/propagation-stores/read-one-synced-store.html).
