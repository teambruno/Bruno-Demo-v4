info:
  name: Read One Password Policy
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/passwordPolicies/{{passwordPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Password Policy](https://developer.pingidentity.com/pingone-api/platform/password-policies/read-one-password-policy.html).
