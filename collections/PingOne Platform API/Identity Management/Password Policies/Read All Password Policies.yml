info:
  name: Read All Password Policies
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/passwordPolicies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Password Policies](https://developer.pingidentity.com/pingone-api/platform/password-policies/read-all-password-policies.html).
