info:
  name: Delete Password Policy
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/passwordPolicies/{{passwordPolicyID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Password Policy](https://developer.pingidentity.com/pingone-api/platform/password-policies/delete-password-policy.html).
