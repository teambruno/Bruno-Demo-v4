info:
  name: Read All (Schema) Attributes
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/schemas/{{schemaID}}/attributes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All (Schema) Attributes](https://developer.pingidentity.com/pingone-api/platform/schemas/read-all-schema-attributes.html).
