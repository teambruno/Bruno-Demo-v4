info:
  name: Update Attribute (Put)
  type: http
  seq: 6

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/schemas/{{schemaID}}/attributes/{{schemaAttrID}}"
  body:
    type: json
    data: |-
      {
          "name": "officeLocation",
          "displayName": "An optional property that specifies the display name of the attribute.",
          "description": "An optional property that specifies the description of the new attribute.",
          "schemaType": "CUSTOM",
          "type": "STRING",
          "unique": true,
          "enabled": false,
          "multiValued": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Attribute (Put)](https://developer.pingidentity.com/pingone-api/platform/schemas/update-attribute-put.html).
