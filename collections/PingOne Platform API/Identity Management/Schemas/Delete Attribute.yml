info:
  name: Delete Attribute
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/schemas/{{schemaID}}/attributes/{{schemaAttrID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Attribute](https://developer.pingidentity.com/pingone-api/platform/schemas/delete-attribute.html).
