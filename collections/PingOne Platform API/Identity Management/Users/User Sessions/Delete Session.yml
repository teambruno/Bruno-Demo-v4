info:
  name: Delete Session
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/sessions/{{sessionID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Session](https://developer.pingidentity.com/pingone-api/platform/users/user-sessions/delete-session.html).
