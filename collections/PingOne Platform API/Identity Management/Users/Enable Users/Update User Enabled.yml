info:
  name: Update User Enabled
  type: http
  seq: 2

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/enabled"
  body:
    type: json
    data: |-
      {
          "enabled": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update User Enabled](https://developer.pingidentity.com/pingone-api/platform/users/enable-users/update-user-enabled.html).
