info:
  name: Read User Enabled
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/enabled"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read User Enabled](https://developer.pingidentity.com/pingone-api/platform/users/enable-users/read-user-enabled.html).
