info:
  name: Remove User from Group
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/memberOfGroups/{{groupID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Remove User from Group](https://developer.pingidentity.com/pingone-api/platform/users/group-membership/remove-user-from-group.html).
