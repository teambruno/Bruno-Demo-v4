info:
  name: Read All Group Memberships for User
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/memberOfGroups?expand=group&limit=100"
  params:
    - name: expand
      value: group
      type: query
    - name: limit
      value: "100"
      type: query
    - name: filter
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Group Memberships for User](https://developer.pingidentity.com/pingone-api/platform/users/group-membership/read-all-group-memberships-for-user.html).
