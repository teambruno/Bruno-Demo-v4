info:
  name: Read All Users in Multiple Groups
  type: http
  seq: 4

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users?filter=memberOfGroups[id%20eq%20%22{{groupID}}%22]%20and%20memberOfGroups[id%20eq%20%22{{groupID_2}}%22]"
  params:
    - name: filter
      value: memberOfGroups[id%20eq%20%22{{groupID}}%22]%20and%20memberOfGroups[id%20eq%20%22{{groupID_2}}%22]
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Users in Multiple Groups](https://developer.pingidentity.com/pingone-api/platform/users/group-membership/read-all-users-in-multiple-groups.html).
