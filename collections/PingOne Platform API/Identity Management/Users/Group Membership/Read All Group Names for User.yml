info:
  name: Read All Group Names for User
  type: http
  seq: 6

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}?include=memberOfGroupNames"
  params:
    - name: include
      value: memberOfGroupNames
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All Group Names for User](https://developer.pingidentity.com/pingone-api/platform/users/group-membership/read-all-group-names-for-user.html).
