info:
  name: Read User Population
  type: http
  seq: 1

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/population"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read User Population](https://developer.pingidentity.com/pingone-api/platform/users/user-populations/read-user-population.html).
