info:
  name: Read Role Assignments
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/roleAssignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read Role Assignments](https://developer.pingidentity.com/pingone-api/platform/users/user-role-assignments/read-role-assignments.html).
