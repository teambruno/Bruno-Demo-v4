info:
  name: Read One Role Assignment
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/roleAssignments/{{roleAssignmentID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read One Role Assignment](https://developer.pingidentity.com/pingone-api/platform/users/user-role-assignments/read-one-role-assignment.html).
