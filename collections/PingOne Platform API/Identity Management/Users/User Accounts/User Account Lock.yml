info:
  name: User Account Lock
  type: http
  seq: 2

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.account.lock+json
  body:
    type: json
    data: |-
      {
        "unlockAt": "2023-06-07T23:59:59Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [User Account Lock](https://developer.pingidentity.com/pingone-api/platform/users/user-accounts/user-account-lock.html).
