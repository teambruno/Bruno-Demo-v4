info:
  name: User Account Unlock
  type: http
  seq: 1

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.account.unlock+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [User Account Unlock](https://developer.pingidentity.com/pingone-api/platform/users/user-accounts/user-account-unlock.html).
