info:
  name: Update Password (Self)
  type: http
  seq: 7

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/password"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.password.reset+json
  body:
    type: json
    data: |-
      {
          "currentPassword": "{{currentPassword}}",
          "newPassword": "{{newPassword}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update Password (Self)](https://developer.pingidentity.com/pingone-api/platform/users/user-passwords/update-password-self.html).
