info:
  name: Delete Linked Account
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/linkedAccounts/{{linkedAccountID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete Linked Account](https://developer.pingidentity.com/pingone-api/platform/users/linked-accounts/delete-linked-account.html).
