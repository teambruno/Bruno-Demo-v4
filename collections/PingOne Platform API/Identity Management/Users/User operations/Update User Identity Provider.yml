info:
  name: Update User Identity Provider
  type: http
  seq: 10

http:
  method: PUT
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/identityProvider"
  body:
    type: json
    data: |-
      {
          "id": "{{idpID}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update User Identity Provider](https://developer.pingidentity.com/pingone-api/platform/users/users-1/update-user-identity-provider.html).
