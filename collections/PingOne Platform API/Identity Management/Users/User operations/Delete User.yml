info:
  name: Delete User
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete User](https://developer.pingidentity.com/pingone-api/platform/users/users-1/delete-user.html).
