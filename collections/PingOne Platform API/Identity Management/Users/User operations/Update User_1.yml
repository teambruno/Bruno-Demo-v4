info:
  name: Update User_1
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}"
  body:
    type: json
    data: |-
      {
          "title": "Manager"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Update User](https://developer.pingidentity.com/pingone-api/platform/users/users-1/update-user-patch.html).
