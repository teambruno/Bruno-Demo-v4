info:
  name: Read User Identity Provider
  type: http
  seq: 8

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/identityProvider"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read User Identity Provider](https://developer.pingidentity.com/pingone-api/platform/users/users-1/read-user-identity-provider.html).
