info:
  name: Send/Resend User Verification Code
  type: http
  seq: 5

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.user.sendVerificationCode+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Send/Resend User Verification Code](https://developer.pingidentity.com/pingone-api/platform/users/users-1/send-resend-user-verification-code.html).
