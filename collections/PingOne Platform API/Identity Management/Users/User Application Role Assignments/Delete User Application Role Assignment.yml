info:
  name: Delete User Application Role Assignment
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/applicationRoles/{{appRoleID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Delete User Application Role Assignment](https://developer.pingidentity.com/pingone-api/platform/users/user-application-role-assignments/delete-user-application-role-assignments.html).
