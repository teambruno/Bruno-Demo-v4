info:
  name: Revoke Agreement
  type: http
  seq: 2

http:
  method: POST
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/agreementConsents/{{agreementID}}"
  headers:
    - name: Content-Type
      value: application/vnd.pingidentity.consent.revoke+json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Revoke Agreement](https://developer.pingidentity.com/pingone-api/platform/users/user-agreement-consents/revoke-agreement.html).
