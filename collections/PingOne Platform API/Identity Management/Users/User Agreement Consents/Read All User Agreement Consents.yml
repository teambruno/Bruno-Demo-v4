info:
  name: Read All User Agreement Consents
  type: http
  seq: 3

http:
  method: GET
  url: "{{apiPath}}/environments/{{envID}}/users/{{userID}}/agreementConsents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: This PingOne collection contains only the requests without documentation. For the official Platform SSO APIs documentation, see [Read All User Agreement Consents](https://developer.pingidentity.com/pingone-api/platform/users/user-agreement-consents/read-all-user-agreement-consents.html).
