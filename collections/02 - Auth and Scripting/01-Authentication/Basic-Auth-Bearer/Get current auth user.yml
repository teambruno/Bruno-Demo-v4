info:
  name: Get current auth user
  type: http
  seq: 3

http:
  method: GET
  url: https://dummyjson.com/auth/me
  auth:
    type: bearer
    token: "{{dummyJsonToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Reusing a captured token

  No script here - the Bearer token is `{{dummyJsonToken}}`, set by the
  `Login` request's `after-response` script. Run `Login` first (or run the
  folder) and this request authenticates with no copy/paste.
