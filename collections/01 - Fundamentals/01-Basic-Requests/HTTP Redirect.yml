info:
  name: HTTP Redirect
  type: http
  seq: 7

http:
  method: GET
  url: https://httpbin.org/redirect/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
