info:
  name: Get Top Product Detail
  type: http
  seq: 2
  tags:
    - smoke
    - regression

http:
  method: GET
  url: "{{dummyUrl}}/products/{{topProductId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const body = res.getBody();

        test("status is 200", () => {
          expect(res.status).to.equal(200);
        });

        test("id matches the chained variable", () => {
          const expectedId = Number(bru.getVar("topProductId"));
          expect(body.id).to.equal(expectedId);
        });

        test("product has stock and price", () => {
          expect(body).to.have.property("price").that.is.a("number");
          expect(body).to.have.property("stock").that.is.a("number");
        });

        console.log(`Fetched: ${body.title} | Price: $${body.price} | Stock: ${body.stock}`);
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.id
      operator: isNumber

settings:
  encodeUrl: true
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Chained Request – Product Detail

  The product ID (`{{topProductId}}`) was set by the previous request's `after-response` script.
  This demonstrates how Bruno CLI runs requests sequentially and shares state via variables.
